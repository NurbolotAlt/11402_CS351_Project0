/// <summary>
/// Finds two numbers in an array that add up to a target sum and returns their indices.
/// </summary>
/// <param name="nums">An array of integers to search through</param>
/// <param name="target">The target sum to find</param>
/// <returns>An array containing the indices of the two numbers that sum to the target</returns>